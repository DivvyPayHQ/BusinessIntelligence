import { ReactElement } from 'react';
import { requireNativeComponent, ViewProps } from 'react-native';

export const BarChart = requireNativeComponent("BarChartModule");
export const LineChart = requireNativeComponent("LineChartModule");

